#include <stdio.h>

typedef char String[6];

int main () {
    // int n, totl = 0;

    // printf("Enter positive number: ");
    // scanf("%d", &n);

    // for (int i = 0; i <= n; i++)
    // {
    //     if (i % 3 == 0 || i % 5 == 0 || i % 7 == 0)
    //     {
    //         totl += i; 
    //     }
        
    // }
    
    // printf("Total: %d", totl);


    return 0;
}