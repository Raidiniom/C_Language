#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

// #include "openhash.c"
#include "closehash.c"

int main() {
    
    // HashTable myHash;
    
    // initDictionary(&myHash);
    
    // insertElem(&myHash, createStudent(1990, "Voltaire", "Tec", 2));
    // insertElem(&myHash, createStudent(2000, "Volkan", "Tec", 4));
    // insertElem(&myHash, createStudent(3092, "Volran", "Tec", 8));
    // insertElem(&myHash, createStudent(1991, "Callen", "Com", 12));
    // insertElem(&myHash, createStudent(1992, "Yousif", "Sys", 20));
    // insertElem(&myHash, createStudent(1993, "Charles", "Com", 3));
    // insertElem(&myHash, createStudent(1994, "John", "Sys", 25));
    // insertElem(&myHash, createStudent(1995, "Ivan", "Tec", 1));
    
    // printf("\nHashTable\n");
    // visualizeTable(myHash);


    
    return 0;
}